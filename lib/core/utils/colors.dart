import 'package:flutter/material.dart';

class AppColors {
  /// This is to be used as the default background for a full screen in Sonara
  static const Color background = Color(0xFF02010A);

  /// This is to be used as a substitute background color shade.
  /// Can be used for dialogs, popups or any other widget depending on your taste.
  static const Color background_2 = Color(0xFF0C0C0C);

  /// This is the main accent color of the app.
  static const Color purple = Color(0xFF8B5CF6);
  static const Color purple_2 = Color(0xFFA855F7);
  static const Color pink = Color(0xFFEC4899);
  static const Color pink_2 = Color(0xFFF472B6);
  static const Color greyBackground = Color(0xFF0C0E07);
}
